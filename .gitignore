/target
**/*.rs.bk
# Runtime logs (e.g. pasture-cost.jsonl) are PII-adjacent and never committed.
*.jsonl
# ...except the curated, PII-free self-improvement ledger (IMP-13).
!IMPROVEMENTS.jsonl
.env
# Test artifact: some unit tests use "unused" as a throwaway cost-log path.
/unused
